Version 5.05
- direct update of exchange rates from Internet.

Version 5.04c
- bug fixed: wrong rate entered for USD when importing
  from a file in format Unit/Home or Home/Unit
- several currencies with the same ticker are all
  updated by import
- faster opening sequence

Version 5.04a
- the minimum number of decimals for calculated amounts
  can now be set in Preferences.

Version 5.04
- 16 currencies now be displayed on the revo screen,
  instead of 12
- bug fixed: one can now enter more than 100
  currencies.
- bug fixed: the correct date of update is set for
  the USD, when importing in format "units per USD".
- bug fixed: the install process does not erase the
  old database if not instructed to do so by the user.

Version 5.03
- now works on revo and most Epoc machines
- 33 currencies per screen on 5mx (12 on revo)
- when installed on the revo, a keyboard is also
  available on screen

Version 5.02a
- fixed a bug in the Exchange rate update function
  (update USA in the format "Units per Home")

Version 5.2
- Monnaie is now "Euro compliant" (see Help)
- added Hotkey: Ctrl+arrow (L or R) >> toggle display
  date of last update, ticker, region, Euro, exch. rate.

Version 5.1
- added a module to import from copies of web pages
  (Message Suite)
- the clock is now 24 hours 

Version 5.0a
* bottom right window is properly erased for long country name
* if a number is keyed in after auto-paste at foreground, the
  input window is refreshed
* small change in help file