Version 5.13n:
- added automatic update of currencies exchange rates to
  the "Import" modules
- added access to Stockwatch.de and Boursorama.com to the
  "Internet Import" module
- dots are now forbidden in names
- some dialog windows have been adjusted to better fit the
  screen of the Revo.
- fixed an obscure bug in "Recalculate"

Version 5.13i:
- new option: direct import of quotes from internet
- bug fixed: Print for the view "All accounts by share,
  ordered by share (amount)".

Version 5.13a:
- modified the view "All accounts by share, ordered by
  category": now the amount held in each share is shown
  as % of its category and as % of grand total...  
- improved speed in Charts, "Delete between dates" and
  "Import from Porfolio Chart"
- bug fixed: in Chart, Import from text file.
- some other minor bugs fixed

Version 5.13:
- new option: FIFO calculation mode for capital gains and
  average costs.
- Import in Charts (from text): now accepts dates with 2
  digits (99 or 00 for 1999 or 2000).
- new functions in Charts: Export to / Import from files
  in Porfolio Chart format (faster than text format).
- new function in Charts: moving average.
- new function: "Share exchange", for eg mergers (now in
  Menu > Transactions > Others).
- one can now enter one comment for each transaction, not
  just account related incomes (Shortcut <Shift+Tab>).
- new screen: all shares in all accounts, ordered by amount.
- smaller spots in Charts.
- more digits for small gain % in Account screen. 
- some bugs fixed in Import/Export QIF files.
- bug fixed: Print when total invested is Zero (eg Options).
- bug fixed: in Update of Sharesplits. 
- bug fixed: cash transfers are now also listed under
  "account related" transactions.

Version 5.12b:
- the selection of the "zoom in" dates range in Charts 
  can now be made directly with the pointer (cross the
  screen between the two dates)
- the "100%" date in the Charts Percent view can now be
  selected directly with the pointer
- the "Zoom in" and "Zoom out" silk screen buttons are
  now effective in Chart. 
- new command in Housekeeping: 'Tidy up all Charts'. 
- new Preference option: default first date for the
  display of Charts.
- <Enter> in Transactions now shows average cost also
  for share transfers.
- the format for the display of dates is no more a
  Preference option: it is read directly from the
  "International" settings of the machine.
- multipages dialogs for "Preferences" and "Performance"
- new "comment" field in Transactions of type "account income"
- in Preferences: now icons only are moved from old to new
  "default folder"
- bug fixed: the effect of a share split on stock options is now
  handled.

Version 5.11:
- new function: IRR calculation (see Help). 
- two more formats for import of quotes files: import
  now accepts semicolon separator, with dot or comma
  as decimal separator.
- new option: when doing a share transfer, one can chose
  valuation at average purchase cost or at last quote.
- new hotkey: <Shift+Right arrow> to display the tickers
  all shares currently displayed (works in the 'Account'
  screen and in the Update all shares screen).
- new hotkey: <Shift+Left arrow> in Account screen,
  detail of quotes mode, to display the average purchase
  costs in the currency of quote (conversion is made at
  the last known exchange rates for the currency of quote
  and the account currency).
- more functions can be accessed from the 'Update all
  shares' screen: delete a share, rename it, change
  its ticker.
- compacting of Chart files is now done automatically as
  and when required (no more optional dialog at exit).
- bug fixed: empty names are no more accepted for
  new formulae.
- bug fixed: wrong calculation of the "average cost" in
  the case of multiple short sales on one share.
- the value of share transfers is now shown in the "cash"
  column in the Transactions screen. This assures
  consistency with the "Net cash in" shown in the "Account"
  screen 

Version 5.10:
- zooming range of dates in Chart is now "sticky"
  when one moves up and down a list of shares with
  the arrows
- new function: automatic calculation of fees and taxes
  (see Help, Transactions: Automatic calculations)
- new command: Print in the Update all shares screen
- new Preference option: number of decimals for the
  display of quotes
- the view "this account by category" is now in the
  currency of the account, not in the user's currency
- bug fixed: now checks if a category of acc. income
  is not the last one before deleting
- bug fixed: display of differences in %, when importing
  in the screen Update all shares

Version 5.09a:
- the value of share transfers (in=plus, out=minus)
  is now added to Net cash in shown in the Account
  screen
- bug fixed: Menu key in screen Update Xch. rates
  gave an error
- bug fixed: wrong calculation of the purchase price
  when Cash was ON and cash balance was zero
- bug fixed: anomalies in the "Update amount"
  screen for incomes.

Version 5.09:
- Porfolio now works on Series 7 and Revo
- the application comes in one single SIS installation
  file
- Charts: added "between dates" for the min-max
  options of tidy up
- Charts: changed horizontal axis to "even" dates
  and added vertical lines
- Charts: added a Memo command
- Bug fixed: after a <Shift+Ctrl+L> command (Summary
  of transactions on all accounts), Porfolio "forgot"
  which account was current.
- calculation of average purchase cost when using a
  currency "other" than the account currency: now
  uses the purchase cost of the "other" currency, not
  the last exchange rate.
- finetuned the short selling module (particularly the
  multicurrency part).

Version 5.08:
- bug fixed: transactions were no more recorded
  on Series 5...
- Import can now read Compuserve Portfolio files
- first version to be translated into French

Version 5.07:
- must now be used with Dbutils.opx
- new command: compact all transactions before...
  (see Help, keyword 'compact')
- new view: summary of all transactions, by share,
  between 2 dates (menu command: Transactions >
  View history > Summary this account).
- new account performance measurement: VPU, or
  Value per Unit (see Help, keyword 'vpu').
- new option: comment field for each share and each
  account (see Help, keyword 'comment' or 'memo').
- new functions: export and import transactions (QIF
  format).
- more progress bars...
- bug fixed: last quote was changed even when a quote
  was entered for a date before last.
- bug fixed in Performance screen ("Overflow in
  Perfobarre").

Version 5.06:
- added a graphical progress control bar during some
  time consuming steps.
- several changes in the "Update all shares" module
  (Ctrl+Q): import command added, % increase displayed,
  automatic next line in manual update, keying in a
  number opens an input window (where "American
  format" quotes like 78 3/8 are accepted)
- new option in Transaction > History: "other share",
  to look for transactions on a share, which is no more
  held in the account.
- bug fixed: delete between dates...

Version 5.05d:
- bug fixed: error when creating a new account...

Version 5.05c:
- new option in the transaction update popup menu:
  "More updates".  If this option is used instead of
  "Return" after a "Change amount", one can update
  more transactions before a full recalculation of the
  account is made, and save time...
- new option in Transaction > History: "account", to
  display only "account related" income/expense items
  and cash transfers.
- new option in Chart Edit > Tidy up: "Delete between
  dates", which is more powerful than the previous
  "Delete all before".
- new option: Account > Record > "All Net Values", to
  reord the Net Values of all accounts at once.
- bug fixed: in All shares quote update screen, the screen
  was not refreshed correctly, when the share had been
  selected with the pointer.

Version 5.05b:
- new function: change base currency of an account.
- new function: tranfer share, options and cash from
  account to account.
- bug fixed: wrong total when several pages were
  printed (history of transactions).
- the summary reports on "all accounts" can now exclude
  some accounts (checkbox in the Update > Name >
  Account dialog window).

Version 5.05a:
- bug fixed in the share transfer command.

Version 5.05:
- direct Print command added in Chart.
- easier access to past transactions on shares, which
  are no more in the account (Transaction > History >
  other share).
- new chart: record of "Total gain" for each account (with
  Cash ON).
- application is now distributed as a SIS file.

Version 5.4a:
- GeofoxOne: the application recognises this machine
  and uses the full screen (4 lines added in main screen).
- the "Update quotes" module has been redesigned: it
  is now much faster + it allows to change currencies
  of quotation
- Euro: it is now possible to change the currency of
  quotation of any share (see Help, keyword Euro)

Version 5.4:
- the promised module for handling the "Euro big bang"
  will come with a later version... but before the D Day!
- the layout of files and icons has been completely
  reviewed: all data files will now be in a
  \system\apps\porfolio\data\ folder, saving the need
  for a \data\ folder in the main system screen. Also
  the default folder for account icons can now be
  selected by the user, and modified at any time with
  the (more) "Preferences" command.
- the upgrade from an earlier version is now more
  user friendly, and the application file is smaller: the
  application is distributed with one single conversion
  program called PorfOnce.opo, which is called by
  Porfolio once only, and then deleted. PorfOnce.opo
  takes care of all database structure changes from any
  previous version.
- new hot key: <Shift+Enter> when a list of shares is
  displayed (all accounts by share, performance,..) shows
  a selector with the list of the accounts, where
  transactions on the highlighted share have been
  recorded. The number of shares presently in hand is
  shown for each account. Note: this hot key has the
  same effect in Chart mode.
- "bug" fixed: commas are now forbidden in all names.
- bug fixed: <Tab> selected the wrong chart in the
  "all shares by category" summary.

Version 5.3c:
- added a "sort by category" option for the "all accounts
  by share" summary report, and the corresponding Print
  command.
- added an "active / inactive" flag for shares.
- added a Preference option on compacting charts at exit
  (always / never / dialog).
- added a Preference option: "Smaller font in List of
  Transactions".
- added an automatic update of exchange rates at startup,
  when Monnaie is installed on the same machine.
- added a hot-key in Chart: <Cursor Down> (resp. Up) opens
  the next (resp. previous) chart in the list at background.
- speed improvement: more screens are memorized before
  the display of summary reports, so that there is less time
  taken rebuilding them on return.
- bug fixed: it was no more possible to "buy" or to "rename"
  a currency.
- bug fixed: zoom had stopped working in some cases.

Version 5.3b:
- account income: all currencies are now allowed.
- Help reviewed: now prints OK through PC.
- bug fixed: in account display, back from "Performance",
  Tab now always produces the right chart.
- bug fixed: dividend > other share now opens share selector.

Version 5.3a
- new module "Performance": to compare the % variations
  of a number of shares over a given period of time.
- new function: find a share in the account screen by
  pressing its initial letter
- display of "account" screen back from "transaction" screen
  is faster, if no update has been made in transactions
- renamed "graphs" to "charts" and "others" to "indices"
- added a "record total value all accounts" command
- "bug" fixed: when updating a quote from the account display
  screen, the whole account is updated if there are also
  options on the share updated
- bug fixed: "number of graphs updated", after import,
  in Charts.
- bug fixed: it was not possible to update name & category
  for a stock option

Version 5.3
- introduced 'categories of shares' and various summary
  reports (by category, by share across all accounts,...)
- introduced 'categories of income' for account related
  incomes and expenses.
- a dividend can be negative (ie share related expense)
- one can record a dividend for a share, which is no more
  held in the account.
- new hot-key in transactions display: Tab => details of
  the highlighted transaction.
- when updating quotes in account view. the totals in the
  bottom window are updated after each quote update.
- the 'Update Names' command is now split into Shares,
  Accounts, Categories,...
- Porfolio comes back to foreground on exit from a memo.
- Help revised and updated.
- bug fixed: there were 2 commands Ctrl+M in Graph.
- bug fixed: updating a purchase transaction in a currency
  other than the account currency was followed by an
  incorrect currency conversion in balances.
- bug fixed: error message on update a share split.
- bug fixed: incorrect treatment of the cash effect of the
  reinvesment of a dividend...

Version 5.2a
- added a "Share options" module
- <Enter> in Transaction display mode now displays average
  unit price for the transaction highlighted, last quote and
  difference in %
- <Esc> in Graph mode returns to account display mode
- "Record Net Value": division by 1000 is now optional
- bug fixed: error message on Import in Graph mode
- bug fixed: error message on "cancel delete share" (housekeeping)
- bug fixed: overflow on "tidy up" for large size Graph files

Version 5.2
- added a 'Print' command from 3 screens
- added a 'Share split' command
- added a 'Dividend reinvestment' option
- added a 'Zoom in' command in Graph mode
- added 'Save as', 'Merge in' and 'Multiply' commands in Graph
  menu
- changed the 'Compact graph files' process: now a series of
  options is offered when exiting the programme
- it is now possible to have Tickers for 'Others'
- rearranged the menu: 'Update names and tickers' is now
  under 'Tools'
- bug fixed: error when deleting currency in Housekeeping
- reviewed Help

Version 5.1a
- added a 'tidy up' command with 3 options in the graph mode
- application cannot be exited by Ctrl+Esc. It is also properly locked in
  all dialogs
- bug fixed: history graphs for Net Value of accounts were not created
  automatically.

Version 5.1
- it is now possible to import historical quotes from a text file to fill
  history graphs (one share or several shares at once)
- the application is now more "pointer friendly" (tap a line to select a
  quote to update, to display the list of transactions on that share, to
  select a transaction to update,...)
- speed has been significantly improved (update of quotes, display of
  graphs,...), thanks to a change in the database structure
- a "lefthanded" screen option is now available (see Preferences)
- a "View" card has been added in the Graph menu: it is now possible
  to see up to 5 data series on one screen, and to compare
  "normalised" values (see "Index" in help)..