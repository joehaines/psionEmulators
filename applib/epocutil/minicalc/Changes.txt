Version 5.10b
- bug fix: "1/x" key now works OK in RPN mode. 

Version 5.10a
- bug fix: function did not work on Series 5, when
  installing on top of a previous version.

Version 5.10
- 12 programmable functions instead of 8, with up to
  5 variables (in RPN mode)
- 5 positions in the RPN stack
- bug fixed: after a calculation, or after a (C)lear,
  a leading decimal separator was not accepted.

Version 5.09a
- checks the decimal separator at first run and installs
  the example functions accordingly
- bug fixed: BusFin input did not work in RPN mode
  after a calculation in the MiniCalc window.

Version 5.09
- new module: "Functions". See Help.
- no more MiniCalc.dbf and BusFin.SIS. All data in MiniBase
- installation with one single SIS file.

Version 5.08
- adapted to the screens of Revo and Series 7
- added a "smaller" option for the size of the keyboard
  (recommended for Revo).

Version 5.07c
- recognizes Ericsson MC218 and its different
 "silkscreen" key pattern
- "%" keypress operates properly in RPN mode

Version 5.07b
- new hotkey: G to display a clock for 2 seconds
- the menu layout now respects the Epoc standards
- this version is uploaded with version 5.00 of Sysram.opx

Version 5.07
- the application is MUCH faster.
- the number of decimals for the display of amounts
  by the currrency converter can be set in Preferences.
- bug fixed: in RPN mode repeated entries starting
  with a decimal separator were incorrectly processed. 

Version 5.06b
- bug fixed: overflow when creating or updating a
  BusFin Screen

Version 5.06
- added a Currency conversion module (hotkey: F)
- redesigned the ÷ (divide) key, to make it look less
  like + (add).
- bug fixed: Ctrl+N and Ctrl+U now do not operate,
  when BusFin is off.

Version 5.05
- added a Business & Finance extension.
- added a Help file.

Version 5.4a
- minor bug fixed: pressing A, S or N before anything at
  startup caused the app to crash.

Version 5.4
- bug fixed: large numbers are now properly rounded to
  avoid hiding the left figure(s)
- the app now "remembers" if the keyboard was displayed
  or not last time
- the full screen area of the Geofox-One is now covered

Version 5.3a
- bug fixed: auto-copy at foreground is now better handled
- bug fixed: in RPN mode Ctrl+V copied the clipboard to
  the display window but not to the stack
- bug fixed: it was not possible to enter several decimal
  separators in RPN mode.

Version 5.3
- RPN calculation mode is now supported
- bug fixed: "A" command after an operation sign caused
  an exit.
- bug fixed: decimal point was not always accepted after
  command "N"

Version 5.2
- MiniCalc can now be move around the screen with the pen.

Version 5.1c
- the application can now be installed on drive d:
- at first run MiniCalc opens on the right side of the screen
- bug fixed: raise to power is no more accepted when the window
  is empty
- bug fixed: serial calculation on long number is now more reliable
  (eg 1234567891+1234567891×...)
- bug fixed: MiniCalc now supports thousands separator at "bring" 

Version 5.1b
- "bug" fixed: serial additions to (& substractions from) memory
  now easier

Version 5.1a
- thousands separators now used
- a pause has been introduced before auto-paste at foreground,
  to allow the exiting application to fill the clipboard
- bug fixed: decimal separator was not accepted in serial
  calculations (!)

Version 5.1
- added Copy/Paste (Ctrl+C/Ctrl+V) functionality, with the options
  to auto-Copy when MiniCalc is sent to the background and to
  auto-Paste when MiniCalc is called to the foreground.