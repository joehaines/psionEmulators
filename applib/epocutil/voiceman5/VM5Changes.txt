Changes to VoiceMan5 since v1.42
================================
* I discovered that it was possible to 'jam' VoiceMan5 in the
background at switch-on if you pressed a SilkScreen key after
VoiceMan5's 'alert' sound had played but before the notification
screen had appeared.  Doing this would make the program still
think it was in the foreground (even though it wasn't) and therefore
not respond to either the hot-key or further switch-ons. I've 
adjusted the code so that this no longer happens.
* Optimised the code to reduce the program's size slightly.

Changes to VoiceMan5 since v1.41
================================
* I realised that the logic change I made for v1.41 only worked
when you first ran the program - instead of every time it detected
a power-on.  In other words, it would only send the Record app.
to the background (outside of an alert period) when VoiceMan5
was first run.  It now correctly sends it to the background (outside
the alert period) both when first run or when it detects a power on.

Changes to VoiceMan5 since v1.4
================================
* Danish and Czech language versions added.

* Whilst VoiceMan5 has always moved the Voice Notes file to
the background for an 'alert' notification, it didn't do this if you'd
just recorded a Voice Note (i.e. the Record app. was in the
foreground when you switched the machine on) but you were
outside the alert period.  I found this irritating and so now the 
program does this in both situations.

Changes to VoiceMan5 since v1.32
================================
* New day & time options.  I realised  that one of the things that
used to stop me using the program sometimes was  the fact that I
tended to use it for work only - and I didn't want to be reminded about
work stuff out of work hours.  The program can now be configured to
remind you about your Voice notes: Any time / Workdays only /
Weekends only and between specified times if desired also.  Read the
Help file for details.

* There's a new 'Sound Alert' option which plays a nice alert sound if
desired at a user-defined volume.  NB: This is designed to only play
during an actual alert.  It won't play if the new Hot-key feature
(Ctrl+Fn+V) is used to pull the Alert dialog to the foreground (see below).

*There's now Hot-key access to the program.  Ctrl+Fn+V triggers the
program to look for your Voice notes.  If found, it brings up the
VoiceMan5 alert screen.  If not found, it brings up the Settings screen
so you can change those instead.

* The program now auto-closes the Help file if it's left open when
VoiceMan5 is quit.

* VoiceMan5 now works on Spanish machines - as well as English,
Dutch, French, German, Italian, Norwegian, and Swedish language
machines!

* Fixed a bug that would crash the program if the Voice notes drive
was switched from c: to d: and back again without the appropriate
'Standard folder' directory existing on d: drive...

Changes to VoiceMan5 since v1.31
================================
* Fixed a bug that'd been irritating me since v1.31 was published.
If the machine was switched of (by closing it) with the VoiceMan5
option dialog in the foreground, it'd usually fail to detect the Owner
info./password screen (if set) and not wait for it to be cleared before
trying to detect either the Esc or Enter keys being pressed.  Hence it
would then (say) detect the Enter button being pressed once the
password had been entered and assume that you wanted the Voice
note(s) played immediately.  This is now fixed and working as it
should.

Changes to VoiceMan5 since v1.3
================================
* VoiceMan5 can now detect and wait for the Owner Info./password
screen at switch on (if enabled).  Previously if this screen was displayed
and/or the password was enabled, pressing Esc to quit the Owner Info.
or Enter to enter your password got carried straight through to VoiceMan5
- causing it in turn to either immediately 'Ignore' or 'Play' the Voice notes
file.
* Now available in Swedish (as well as Dutch, English, French, German
and Italian).

Changes to VoiceMan5 since v1.22
================================
* If you choose to change drives for recording your voice notes on,
VoiceMan5 will now close the Voice notes file if it is open.  Previously,
if the Voice notes file was already open and you changed drives without
closing it; then the machine would automatically default to the already-
open file without looking for a file on the new drive.
* When you change drives, VoiceMan5 will now give you the option to
copy or move your existing Voice notes file to the new drive.  This means
that you can now more easily run 2 Voice notes files - one on each drive -
and swap between the two as required.  I'm indebted to Michael Maehriein
for his help in suggesting the above fixes/ammendments.
* Improved the responsiveness of the program at start-up and added more
dialog to give better user feedback.
* When you quit VoiceMan5 from the 'You have Voice note(s) pending'
dialog, it will now quit to the application shown underneath it instead of
returning you to the System screen.

Changes to VoiceMan5 since v1.21
================================
* Arjen Hiemstra was kind enough to point out to me that the program wasn't
displaying any of the 'About' info. properly (and actually crashing if the
'About' info. was requested after a switch-on/Voice Note alert.  I realised
that I'd forgotten to package the VoiceMan5.mbm file in the .sis file that
I'd published.  I hadn't noticed this before because - when testing it - I
was installing the program onto the already-existing VoiceMan5 folder on my
machine (which already had the missing .mbm file in it!).  My apologies to
all concerned for my stupidity!  :촲

Changes to VoiceMan5 since v1.2
===============================
* A few days after releasing v1.2, I discovered that it was still possible
to Send VoiceMan5 to the background (when it alerted you to a new Voice
Note) by tapping on the screen around about the alert.  In fact, this brings
the app. (or system screen if appropriate) immediately behind it to the
foreground - but VoiceMan5 couldn't detect this properly and would 'get
jammed' as it used to in previous versions.  This has now been fixed.

Changes to VoiceMan5 since v1.13
================================
* Previously when VoiceMan5 alerted you at switch-on to  new Voice
Note(s), it was possible to bypass the program's 'Ignore Voice Note' function
by pressing a System bar key or by using a task switching program.  This
would send VoiceMan5 to the background while still waiting for your
instructions - and effectively disable it from coming to the foreground
again at the next switch-on.  v1.2 is now 'more aware' of what's going
on around it and can detect this move to background, reset itself, and be
ready for the next switch-on it detects.  This effectively makes VoiceMan5
more intuitive to use and 'user friendly'!  ;-)

Changes to VoiceMan5 since v1.12
================================
* Mario Collado has finished the German Help file and so this has been added
into the German version of the program.

Changes to VoiceMan5 since v1.11
================================
* Added the FreEPOC information screens.
* Added the French resource and help files - with greatful thanks to Brunel
Merleau-Ponty and Christophe Goetzmann for doing the translation.

Changes to VoiceMan5 since v1.1
===============================
* Minor bug fix.  Whilst the program usually closed and re-started okay
(i.e. automatically) when backed up with PsiWin 2.3, it didn't work properly
with John Boyce's excellent JB5StopGo program (which I use a lot!).  This
has now been tweaked so that it'll both work with John's program and always
work with PsiWin 2.3.

Changes to VoiceMan5 since v1.0
===============================
* Multiple language capability.  Within a couple of days of the release of
v1.0, I was asked if a German version could be generated (the program is
as much language-specific to the machine as the user!).� This has been done
by creating a resource file for the program using Guillaume Sabouraud's
excellent freeware apps. that will generate this resource file automatically
from an ordinary S5 Data file and then adapting VoiceMan5 to use it.� See the
DIY Tanslations page on my web site for details of how it's done for Clock5.
My thanks to Mario Collado for doing the German translations.� (NB: Mario is
working on the program's Help file and hopefully this will be added to the
German version in a couple of weeks or so.).  This also means that it will now
be very easy for anyone to generate their own language versions of VoiceMan5.
If anyone would care to download and translate the two S5 Data files on my
web site and then send them to me, I'll generate their own language version
and send it back to them (and publish it) by return.� Any takers for French,
Italian, Spanish, etc.?