#define VARES_OPH

#ifndef VARRAY_OPH
#include <sdk\varray>
#endif

/* Property of vares */

STRUCT PRS_VARES
	data%
ENDS

STRUCT PR_VARES
	root#(SIZEOF(PRS_ROOT))
	varoot#(SIZEOF(PRS_VAROOT))
	vares#(SIZEOF(PRS_VARES))
ENDS
