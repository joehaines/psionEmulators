Switch2.zip (2337 bytes)

Enhancement of the original Switch.Zip by Ren‚ Verschoor,
Switch2:

  - works properly when mains power is on when first started;
  - traps its termination and restores original timeout;
  - less than half the size, and uses much less memory (<2K);
  - image file rather than an application;
  - operates entirely in the background at low priority;

C source and executable included.

David.

